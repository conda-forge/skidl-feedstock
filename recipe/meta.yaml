{% set name = "skidl" %}
{% set version = "0.0.29" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: 0e324ccd90fb0c5eb42110678f35f77764b87e4390b574e42cab893bfdc3fa21

build:
  noarch: python
  number: 0
  script: "{{ PYTHON }} -m pip install . -vv"
  entry_points:
    - netlist_to_skidl = skidl.netlist_to_skidl_main:main

requirements:
  host:
    - python
    - pip
  run:
    - python
    - kinparse >=0.1.0
    - enum34
    - python-graphviz
    - wxpython
    - future

test:
  imports:
    - skidl

about:
  home: https://github.com/xesscorp/skidl
  license: MIT
  license_family: MIT
  license_file: LICENSE
  summary: 'SKiDL is a module that allows you to compactly describe the interconnection of electronic circuits and components using Python.'

extra:
  recipe-maintainers:
    - hmaarrfk
